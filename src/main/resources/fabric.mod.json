{
  "schemaVersion": 1,
  "id": "stackable",
  "version": "${version}",
  "name": "stackable",
  "description": "Fix handling of stacks above vanilla maximum stack size",
  "authors": [
    "Finn Steffens (0xhtml)"
  ],
  "contact": {},
  "license": "GPL-3.0",
  "environment": "client",
  "mixins": [
    "stackable.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
